# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

# google oauth credentials (NEVER commit)
google-oauth-client.json
google-oauth-token.json

# IndexNow local state (key is public anyway but kept out of git for hygiene)
.indexnow-key

# Archived old logos (kept locally only — avoid confusing AI design tools that scan the repo)
public/_archive-logos/
public/images/_archive-logos/
